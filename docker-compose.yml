version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./certvault.db:/app/certvault.db
    environment:
      - SECRET_KEY=${SECRET_KEY:-changeme}
      - DATABASE_URL=sqlite:///certvault.db
    restart: unless-stopped
